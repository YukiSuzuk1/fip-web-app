import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Output as static export for Vercel (default is Node.js serverless)
  // Using default Next.js output for Vercel deployment
};

export default nextConfig;
